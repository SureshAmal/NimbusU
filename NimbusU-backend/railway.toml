[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python manage.py migrate --noinput && daphne -b 0.0.0.0 -p ${PORT:-8000} nimbusu.asgi:application"
healthcheckPath = "/api/v1/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
